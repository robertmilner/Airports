<%= form_for(@flight) do |f| %>
  <% if @flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flight.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @flight.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :departure_airport_id %><br />
    <%= f.collection_select :departure_airport_id, Airport.all, :id, :city %>
  </div>
  <div class="field">
    <%= f.label :arrival_airport_id %><br />
    <%= f.collection_select :arrival_airport_id, Airport.all, :id, :city %>
  </div>
  <div class="field">
    <%= f.label :departs_at %><br />
    <%= f.datetime_select :departs_at %>
  </div>
  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
